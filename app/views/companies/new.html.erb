<div class="space-y-6">
  <!-- Page Header -->
  <div class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-6">
    <div class="flex items-center justify-between">
      <div>
        <h1 class="text-2xl font-bold text-gray-900 dark:text-white">Create New Company</h1>
        <p class="text-gray-600 dark:text-gray-300 mt-1">Set up a new company in the system</p>
      </div>
      <div class="text-right">
        <%= link_to "Back to Companies", companies_path, 
            class: "inline-flex items-center px-4 py-2 border border-gray-300 dark:border-gray-600 shadow-sm text-sm font-medium rounded-md text-gray-700 dark:text-gray-300 bg-white dark:bg-gray-800 hover:bg-gray-50 dark:hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
      </div>
    </div>
  </div>

  <!-- Company Creation Form -->
  <div class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700">
    <div class="px-6 py-4 border-b border-gray-200 dark:border-gray-700">
      <h2 class="text-lg font-medium text-gray-900 dark:text-white">Company Details</h2>
      <p class="text-sm text-gray-600 dark:text-gray-300">Enter the basic information for the new company</p>
    </div>

    <%= form_with model: @company, local: true, class: "divide-y divide-gray-200 dark:divide-gray-700" do |form| %>
      <div class="px-6 py-6 space-y-6">
        <!-- Basic Information -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div>
            <%= form.label :name, class: "block text-sm font-medium text-gray-900 dark:text-white mb-2" %>
            <%= form.text_field :name, 
                class: "block w-full border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                placeholder: "e.g., Acme Corporation" %>
          </div>

          <div>
            <%= form.label :domain, class: "block text-sm font-medium text-gray-900 dark:text-white mb-2" %>
            <%= form.text_field :domain, 
                class: "block w-full border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
                placeholder: "e.g., acme.com" %>
          </div>
        </div>

        <!-- Settings -->
        <div>
          <%= form.label :settings, "Company Settings (JSON)", class: "block text-sm font-medium text-gray-900 dark:text-white mb-2" %>
          <p class="text-sm text-gray-600 dark:text-gray-300 mb-3">Optional JSON configuration for company-specific settings</p>
          <%= form.text_area :settings, 
              rows: 6,
              class: "block w-full border-gray-300 dark:border-gray-600 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 sm:text-sm font-mono bg-white dark:bg-gray-700 text-gray-900 dark:text-white",
              placeholder: '{"timezone": "America/Chicago", "work_hours": {"start": "9:00", "end": "17:00"}}' %>
        </div>
      </div>

      <!-- Form Actions -->
      <div class="px-6 py-4 bg-gray-50 dark:bg-gray-700 border-t border-gray-200 dark:border-gray-600">
        <div class="flex justify-between items-center">
          <%= link_to "Cancel", companies_path, 
              class: "inline-flex items-center px-4 py-2 border border-gray-300 dark:border-gray-600 shadow-sm text-sm font-medium rounded-md text-gray-700 dark:text-gray-300 bg-white dark:bg-gray-800 hover:bg-gray-50 dark:hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
          
          <%= form.submit "Create Company", 
              class: "inline-flex items-center px-6 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        </div>
      </div>
    <% end %>
  </div>

  <!-- Help Information -->
  <div class="bg-blue-50 dark:bg-blue-900 border border-blue-200 dark:border-blue-700 rounded-lg p-6">
    <div class="flex">
      <div class="flex-shrink-0">
        <svg class="h-5 w-5 text-blue-400 dark:text-blue-300" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
        </svg>
      </div>
      <div class="ml-3">
        <h3 class="text-sm font-medium text-blue-800 dark:text-blue-200">About Company Creation</h3>
        <div class="mt-2 text-sm text-blue-700 dark:text-blue-300">
          <p class="mb-2">When you create a company:</p>
          <ul class="list-disc list-inside space-y-1">
            <li>A company administrator account will be created automatically</li>
            <li>You can then add users and teams to the company</li>
            <li>Company settings can be configured for timezone, work hours, etc.</li>
            <li>Chat integrations can be set up for team communications</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>
